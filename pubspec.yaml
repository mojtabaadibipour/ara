name: word_learning_app
description: A Flutter app for learning words.

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.4.2

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  assets:
    - assets/B NAZANIN.TTF